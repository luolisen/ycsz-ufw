# YCSZ 教育机房防火墙交付计划

目标：交付 C# / .NET Framework 4.5 兼容的原生 WinForms 客户端、管理端、Windows 服务及 NSIS 安装包。运行支持范围先限定 Windows 10/11 x64、Windows PowerShell 5.1；只在受控授权机房使用。

- [x] Phase 0：检查仓库、远端与环境（空仓库；macOS；Mono 6.14.1、NSIS 3.12 已补齐）。
- [x] 数据模型、安全存储、协议、规则验证与独立测试。
- [x] 客户端服务：进程、代理、网络基线、hosts、出口控制、事件排队与恢复。
- [x] 管理端服务与 GUI：TLS、客户端注册、策略下发、逐机解冻与网络配置。
- [x] 客户端托盘、密码验证、安装初始化与受保护的卸载入口。
- [x] 交叉编译、生成安装包、自动测试、交付文档和 Windows 验证脚本。

## 决策和边界

- 使用系统正常 UAC、服务 ACL 和 SCM 故障恢复。保留管理员恢复/卸载路径与控制面板登记，不隐藏持久化，不绕过管理员权限。
- 不自动推送、发布或改远端。不在当前 Mac 修改网络或启动防护策略。
- 进程匹配按用户给出的完整文件名，不把名称识别等同于所有代理检测。
- IP 出口限制不能证明 HTTPS 目标域名，共享 CDN IP 上存在域名串用边界；必须在界面和文档说明。预置教育/公共服务域名为候选集，不宣称完整或已实时核验的 ICP 名单。
- 删除的硬件或第三方虚拟网卡不能凭配置快照重建驱动；记录不可恢复状态，保留恢复材料，禁止谎报回滚成功。
- Windows 系统调用实效需要 Windows VM/实机；本机完成编译、纯逻辑测试和安装包结构检查，不能作为 Windows 运行验收。

## 验证输出

`docs/SECURITY-VALIDATION.md`：攻击面、复现步骤、已执行结果、风险和实机缺口。
`artifacts/`：可安装程序、校验和与测试输出。

## 尚未完成的部署验收

- [ ] 在 Windows x64 VM/实机执行 W01–W20，验证服务、WFP、GUI、网络回滚与卸载。
- [ ] 严格 HTTPS 域名隔离及官方 ICP 名单核验（当前 IP 方案不等价）。

本机证据：34 项逻辑/布局测试通过、2 项真实回环 TLS 测试通过、PowerShell 语法通过、NSIS 安装包已生成。源码和安装包是工程预览交付，不是生产上线验收。

后续复核：修复 hosts 删除恢复，减少未变化 DHCP/DNS 写入，新增 9 项隔离网络逻辑测试通过。

复核修复：远程网络命令按版本只自动尝试一次，避免失败后反复断网；只在管理端安装入站规则；关键安装/卸载失败停止并保留恢复入口。
