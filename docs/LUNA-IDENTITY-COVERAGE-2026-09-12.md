# 第五轮：初始化的逐文件覆盖证明

主任务已确认dcf2cb5 CI34661290471成功，并亲自修复稳定Active的可信服务CREATE：CREATE无rename目标，不能要求DestinationProtected；仅已识别产品source，仍先受初始化与祖先限制。新增16项实际pre-operation函数测试通过。保留此次与全部历史修复。

## 实施范围

1. 当前YcpRecordInitializationStream仅累计成功事件，Commit仅比较MarkedEntries >= ExpectedEntries。重复打开同一个文件会增加计数，无法证明每个扫描成员标记成功。改为本次初始化、固定双根内的逐成员覆盖确认；以真实句柄身份与驱动自身观察相匹配。不能仅改成数值相等或在用户态去重。重复打开、旧初始化/旧上下文、其他进程、漏标记/名称解析失败均不能伪造覆盖。目录和多个卷需正确处理；超时/Abort/服务退出/卸载清理资源。计数溢出、资源上限和并发应有明确失败行为，不能全系统拒绝I/O。
2. 先写可审查设计，检查真实Windows API语义，必要时升级协议全部端点。不要把过滤器上下文的成功设置次数当成扫描集合。保留合法缓存/paging I/O、精确目录范围、祖先限制、句柄属性检查、映射未满足的诚实状态。
3. 将关键回归测试绑定实际生产函数；当前initialization_lifecycle.c是独立模型而非生产实现，不能以模型通过宣称驱动状态机通过。至少覆盖重复一个文件不能填另一个文件缺口、旧初始化不能贡献、新一轮重新确认、扫描失败不激活、合法服务创建与初始化创建限制。
4. 完整构建和真实Windows WDK CI，修至通过；动态签名加载仍须如实列BLOCKED，不改生产或系统设置。清点仍无法在现有条件完成的具体外部条件，不虚构测试证据。

## 协作与边界

仅/Users/alan/.codex/worktrees/6be0/ycsz-ufw，现有codex/luna-full-delivery-20260912分支。保留全部未提交工作。主任务派发后停止源码编辑，你独占。普通提交推送，先检查上一批主任务CI。不创建新代理。不重启/关机/注销，不改账户网络联想平台/SecureBoot/HVCI/签名强制，不安装未验收驱动到生产。完成后通知主任务01a08c7b-582e-7391-9801-11c48fb0ac74最终commit、CI和证据、剩余缺口并停止编辑，由主任务修bug验收。
