# 用户确认：无驱动保护与异常恢复

用户已确认按无驱动方案实施。此决定替代“必须通过自研内核驱动抵抗提权管理员”的交付目标。基线4dc7e4b，工作树6be0。历史驱动源码保留，不再投入签名、驱动加载或内核测试路线。

## 验收目标

- 核心为正常安装提权的LocalSystem服务；普通学生账户无法结束核心服务进程、停服或修改/删除本产品安装文件及敏感配置。
- 托盘是用户会话入口，允许以监督恢复满足可用性，不谎报同用户托盘不可结束；异常退出后恢复，核心服务有SCM恢复。
- 教师认证后能够正常维护、更新、卸载；标准用户不能通过IPC、命令行或更新路径绕过权限。
- 不承诺抵抗完整管理员、SYSTEM或离线磁盘修改；UI和README准确表述无驱动状态，不再把未安装驱动显示为整个软件故障，也不谎报内核保护。
- 保留通用客户端、计算机名命名与克隆独立身份、管理端桌面快捷方式、.NET4.8内置/非内置包和导出选项。

## 已观察到的代码

installer/Ycsz.nsi已有安装目录SYSTEM/Administrators完全控制、Users读取执行、服务DACL及SCM失败恢复；Program.cs根据驱动注册控制CanStop，但无驱动状态仍经过SelfProtectionCoordinator及驱动导向状态输出。必须先核实现有行为，避免重复实现或破坏原维护流程。

## Luna实施

先做一次完整差距清单，再最小改动补齐：安装目录/ProgramData ACL精确到产品对象、拒绝普通用户危险服务操作、必要的进程DACL（仅产品进程，保留合法管理员维护）、托盘监督及服务恢复、无驱动状态呈现、认证维护和安装升级回滚。勿通过全局策略改变学生账户或其他软件。检查ACL失败必须报错，避免只调用命令忽略结果。产品密码不等于能抵抗OS管理员。

Windows隔离测试需实际证明：标准权限token对产品服务进程终止和文件删除/覆盖被拒绝；明确现有CI是否能提供这样的token，不能以管理员测试或字符串门禁冒充。对可丢弃测试实例证明托盘/服务退出后恢复，以及认证维护不会与恢复机制打架。核心pid不变与新pid恢复分别记录。无可用真实桌面会话时托盘桌面验收列待测。针对新增逻辑测试后跑对应Windows CI，普通提交推送，不新建代理、不每个小问题重新派轮次。

## 交接和生产

Luna独占代码，完成后向主任务01a08c7b-582e-7391-9801-11c48fb0ac74交接并停止编辑；主任务亲自审查修bug，之后按已有学生一/教师端授权使用向日葵技能进行不重启验收和必要产品更新。不得自动重启/关机/注销，不改现有账户/网络/联想平台/系统签名/SecureBoot/HVCI。不使用漏洞提权、系统进程注入、进程隐藏或蓝屏式自保护。未验收前不正式发布。保留全部未提交文档和驱动历史，不包含凭据。
