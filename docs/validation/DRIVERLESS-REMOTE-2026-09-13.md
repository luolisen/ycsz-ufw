# 无驱动交付复审与远程验收

基线33fe669c18a95cba173fa7a59af0130257eab540。主任务已核实Actions 34717473181成功及实际日志：43项Windows隔离检查、SCM异常终止后新PID恢复、设备身份和名称/撤销状态持久化通过。标准用户token被拒绝读取凭据、覆盖程序/脚本、删除脚本/凭据、请求SCM停止/改配置/删除、取得服务进程终止句柄。此证据属于CI隔离Windows，不等于生产实机全部通过。

IPC复审：维护命令进入HostService前由IpcServer验证15分钟会话；无驱动维护为独立5分钟同会话授权；停服发送回复后再次核验维护租约。无驱动默认不构造驱动协调器，保留历史驱动路径但不部署。

## 本次冻结包

通过CI的NSIS客户端安装器提取核心文件，与SHA256SUMS-WINDOWS记录匹配。Ycsz.exe SHA256：970ab485926c096aa50e8d557fa535d215d926f9ebbb8de42775cc9f3758a247；Core：edcc9dc6146bd50e04d189e438440a22d5ab3aedd87480d7897e3b732f74d65c。

本地完整包/Users/alan/Downloads/ycsz-driverless-update-0913；学生精简包/Users/alan/Downloads/ycsz-student-driverless-0913。完整包包含内置/非内置运行库两种客户端安装器。更新脚本使用现有Update-Installed.ps1，不重跑首次配置，不写凭据。

## STU01学生1

已通过向日葵连接并读取hostname、whoami /groups。当前账户属于Administrators，终端High Mandatory Level：本轮无驱动标准用户边界不能承诺抵抗该账户；不修改账户。更新前服务PID6328会话0、托盘PID6564会话1，路径C:\Program Files\YcszFirewall\Ycsz.exe。安装目录Users RX、Administrators/SYSTEM F；注册角色client、版本1.0.1，程序仍为历史585e294b...版本。

学生更新目录已传输至C:\Users\Lenovo\Desktop\ycsz-student-driverless-0913。已启动现有更新脚本，最终结果待核验。教师端尚未更新。本次未重启/注销/关机，不改账户网络联想平台或签名策略。未正式发布。

更新脚本已成功返回PASS：远程安装哈希匹配、配置身份/基线保持；备份C:\ProgramData\YcszUpdateBackup-20260913-045252。再次inspect核对程序哈希970ab485...、角色client、无驱动状态日志；服务PID3584会话0、托盘PID4060会话1。已定向结束托盘4060，服务3584仍存活，正在等待恢复验收。

托盘恢复已实测：定向结束会话1托盘4060后，服务3584保持不变，新托盘7740出现；展开托盘可见盾牌图标，右键“打开管理界面”能打开密码框。此为结束后恢复证据，绝非拒绝结束。

学生端通过原管理密码登录客户端窗口，无超时；状态显示STU01及无驱动边界。认证维护打开后显示最长5分钟授权，点击结束后授权状态消失，再锁定关闭。未在生产删除文件或强杀核心服务。

## 教师端DESKTOP-1GDR6B7

向日葵已连接；本次hostname输出DESKTOP-1GDR6B7。完整更新目录正在传输至教师端桌面，尚未启动教师端更新。远控Return在教师端未执行命令，改用typeText CRLF后只读hostname成功；不永久更改输入法或系统设置。

教师端更新已返回PASS：核心文件与两种客户端安装器逐项哈希匹配，配置身份保持；备份C:\ProgramData\YcszUpdateBackup-20260913-050018。更新后再次inspect显示manager角色、程序哈希970ab485...、无驱动日志；服务PID14312会话0，托盘PID24276会话2。

教师桌面YCSZ Manager快捷方式实测能打开登录窗口，原密码登录成功；管理端列表中STU01在线并有本次心跳。生成客户端对话框显示“自动采用各自的计算机名称”，内置.NET Framework4.8复选框可选/取消。两种安装器字节已由更新脚本逐项校验；本次生产仅检查导出界面，未新增接入凭据或实际生成ZIP，实际双包导出证据来自本轮Windows隔离CI。退出导出框并锁定关闭管理窗口，后台服务保留运行。

## 本轮结论与边界

无驱动代码及Windows隔离权限/认证/恢复测试通过；学生和教师两端均已更新到同一33fe669程序哈希。学生托盘恢复、原密码登录、认证维护、教师快捷方式/原密码登录/学生在线/运行库选择实机通过。没有重启、注销或关机；没有变更账户、全局安全策略、联想平台，也没有部署历史驱动。源代码未发现需要本轮再次修改的确定性缺陷；README校正测试数量和本轮证据。

不宣称抵抗完整管理员：学生1现有账户为管理员，Windows隔离标准用户拒绝测试不能外推到该账户。没有对生产真实程序文件做删除，也没有强杀生产核心服务；SCM强杀恢复证据来自CI。旧系统首次安装.NET4.8并重启、镜像批量下发、离线修改、完整Windows管理员对抗等均不属于本轮已通过项。未正式发布新版本；不得把本报告扩写为“全部功能实机验收”。
