# YCSZ 教育机房防火墙：任务梳理与执行规格

日期：2026-09-10  
仓库：https://github.com/luolisen/ycsz-ufw.git  
工作目录：当前 Codex 工作树；不自动推送、合并或发布远端。

## 1. 目标与交付

制作纯 Windows 原生桌面软件，包含客户端、管理端及安装程序。选择 C#、WinForms、Windows 服务，编译目标为 .NET Framework 4.5 API；首轮运行支持限定 Windows 10/11 x64 + Windows PowerShell 5.1 + .NET Framework 4.8（可运行 4.5 目标程序）。不使用 Electron、网页套壳或云端服务。

交付内容：

1. 客户端：常驻服务、托盘、密码管理界面、网络基线、防护、事件上报。
2. 管理端：后台接入服务、密码管理界面、客户端列表、统一白名单、单机解冻/冻结、单机网络配置及事件。
3. 安装程序：选择客户端/管理端角色、设置密码、初始化配置、安装服务与自动启动；提供可恢复的卸载路径。
4. 完整源码、构建脚本、自动测试、安装及操作说明。
5. 攻击面、验证步骤、结果记录、已知风险与 Windows 实机验收清单。

当前构建机为 macOS ARM64。本机实际完成 Windows 程序交叉编译、安装包构建、纯逻辑测试与静态检查；不能把这些结果写成 Windows 服务/网络防护已实机通过。缺少 Windows 测试机时，保留明确的“未验证”状态。

## 2. 功能清单与验收标准

### 2.1 代理进程抓取

按不区分大小写的完整进程文件名匹配，发现后记录名称、PID、时间、处置结果，上报管理端并尝试终止。失败必须记录错误；不得声称能通过文件名检测所有改名代理程序。

| 核心/程序 | 进程名 |
| --- | --- |
| V2Ray | v2ray.exe |
| Xray | xray.exe |
| Shadowsocks-libev | shadowsocks-libev.exe |
| ShadowsocksR | server-win.exe |
| Trojan | trojan.exe |
| Hysteria 1/2 | hysteria.exe、hy.exe |
| tuic | tuic-client.exe、tuic-server.exe |
| Sing-box | sing-box.exe |
| Mihomo / Clash.Meta | mihomo.exe、clash.exe |
| NaiveProxy | naiveproxy.exe |
| v2rayN | v2rayN.exe |
| Clash for Windows | clash for windows.exe |
| Clash Verge | clash-verge.exe |
| Mihomo Party | mihomo-party.exe |
| Netch | netch.exe |
| Furious | furious.exe |
| Hiddify | hiddify.exe |

验收：在授权 Windows VM 中使用无害测试进程验证命中、大小写、非命中、终止失败、离线排队与上线报告。

### 2.2 系统代理检测

检测已加载用户配置中的手工代理、PAC/自动代理设置，以及 WinHTTP 代理。发现非直接连接配置时记录并关闭。服务必须处理当前用户会话之外已加载的用户配置。对未加载用户注册表配置的覆盖边界需说明。

验收：设置手工代理、PAC、WinHTTP 代理后，分别确认检测事件、实际关闭及失败报告。

### 2.3 网络、网卡与 hosts 基线

安装初始化时保存当前网卡身份、启用状态、DHCP/静态地址、DNS、静态路由、协议绑定与 hosts 原始字节。基线保存在仅 SYSTEM/管理员可修改的应用数据目录。

后续检测配置漂移、新增/缺失网卡、虚拟网卡和桥接相关绑定、hosts 修改；自动恢复可恢复的配置，并逐项记录结果。DHCP 租约地址等动态状态不作为静态配置漂移。

本地密码管理页与管理端均可授权更新特定客户端的网络基线。远程变更采用版本号、实际应用结果与客户端回报区分“下发”“应用”“成功”。配置必须验证，不能拼接到任意命令执行字符串中。

验收：静态 IP、DNS、hosts、网卡禁用、新增虚拟网卡、绑定变化分别验证。已删除硬件/驱动不能凭快照重建，记录不可恢复及人工恢复需求，不伪报成功。

### 2.4 网络访问白名单与解冻

统一下发准确域名、IP/CIDR；不接受含路径的 URL、任意脚本或 /0。预置少量教育、政务及公共信息域名供管理员审阅。名单不等于“全部简中备案互联网”，ICP备案状态不凭域名猜测，未逐一在线核验的候选明确标记。

通过 Windows Filtering Platform (WFP) 对 IPv4/IPv6 出站连接施加独立规则：默认阻断，放行名单对应地址及必需的管理通信、DNS/DHCP与回环。只管理本产品自己的 WFP 对象，避免覆盖已有系统防火墙策略。

管理端可对指定客户端解冻全部外网出口限制，再次冻结重新施加规则。解冻仅改变出口策略，进程、系统代理、网络基线防护继续执行。

阻断报告来自 Windows 过滤平台审计事件，注明目的 IP/端口及应用；不能把 IP 日志伪装成已识别的 HTTPS URL。

**关键边界**：域名解析到 IP 的方案不能严格区分共享 CDN IP 上的其他域名，也不能验证网站的 ICP 属性；若要求严格的逐域名 HTTPS 隔离，需要额外的受控应用代理/网关方案。本次必须在界面、说明和验收中公开这一限制，不能宣称已实现严格网站身份鉴别。

验收：白名单 IP、非白名单 IPv4/IPv6、DNS、管理通道、特定客户端解冻与再冻结、重启后规则、审计报告；共享 CDN 反例列入风险记录。

### 2.5 原生界面

采用蓝白色、冻结/解冻状态卡片与清晰操作区，参考冰点还原的状态表达，不复制其商标或素材。

客户端正常仅有托盘图标；按住 SHIFT 点击才弹出密码输入。只输入密码，不设用户名。每次重新打开管理界面重新验证。管理端每次打开同样输入密码。

本地页：防护状态、服务状态、基线查看/授权更新、事件查看、卸载入口。

管理页：客户端列表、最近在线时间、策略应用状态、事件、统一白名单、逐机解冻/冻结、单机网络配置、客户端注册包。

### 2.6 密码与通信

安装时设置管理密码；禁止明文保存密码。使用随机盐 + PBKDF2 密码派生、常量时间比较与失败退避。用户输入只在必要的内存与本机管道请求中使用，不写日志。

服务配置及节点凭据使用 Windows DPAPI 保护，并设置目录 ACL。客户端注册包使用密码派生密钥的 AES-CBC + HMAC-SHA256（先认证后解密）。管理通信采用 TLS 1.2、预置证书 SHA-256 指纹及逐机随机令牌，不开放未认证策略写入接口。

验收：正确/错误密码、盐随机性、加密包篡改、错误证书、错误令牌、超长/非法消息、未认证本地命令拒绝。

### 2.7 提权、常驻、恢复和卸载

安装经正常 UAC 获取管理员权限。防护在 LocalSystem 服务中运行，由 SCM 配置开机自动启动和失败重启；普通学生账户不能停止服务或修改配置。非正常退出后的再次启动必须记录并报告。

用户提出的“自动提权、防杀、写注册表防卸载、控制面板隐藏”按以下透明管理边界实现：**不绕过 UAC，不阻止合法本机管理员恢复，不隐藏安装记录，不写隐蔽持久化**。使用正常服务权限、注册的安装信息和密码管理页卸载入口。保留受控管理员恢复工具，以免网络策略错误造成永久锁机。

验收：普通用户权限隔离、服务意外退出与 SCM 恢复、托盘退出不影响服务、密码管理页卸载、管理员紧急恢复、卸载只移除自身规则并恢复审计设置。

## 3. 组件设计

- `src/Ycsz.Core`：数据模型、密码、注册包、网络协议、规则验证。
- `src/Ycsz.App`：服务、WinForms、安装初始化、Windows 系统集成。
- `src/Ycsz.Tests`：可跨平台运行的独立逻辑测试。
- `scripts`：构建、Windows 系统配置辅助与验证脚本。
- `installer`：NSIS 安装定义。
- `docs`：操作、架构、验证与风险说明。
- `artifacts`：构建输出、安装包、测试日志、校验和。

客户端周期性推送事件和状态，管理端回复当前策略。管理端不需要主动进入客户端端口。离线继续执行最近策略并排队事件，恢复连接后上传。所有文件和通信设定大小/数量上限，防止异常流量无限占用内存和磁盘。

## 4. 执行顺序

1. 完成本任务规格（本文件）与 `PLAN.md`。
2. 完成核心模型、安全存储、协议与自动测试。
3. 实现 Windows 服务、网络基线、代理及进程防护、WFP 与审计。
4. 实现管理端服务、客户端注册、策略及事件同步。
5. 实现原生 GUI、初始化、密码验证及卸载恢复。
6. 编译、构建安装包、运行本机测试，修复实际发现的问题。
7. 输出操作说明、攻击面与验证记录、Windows 实机待验清单及交付文件。

## 5. 当前状态与缺失参数

仓库初始内容为空。已补齐 Mono 6.14.1、NSIS 3.12，完成客户端/管理端源码、34 项自动测试、2 项回环 TLS 测试及工程预览安装包；进度见 PLAN.md。Windows 功能验收和严格域名/ICP备案要求仍有明确缺口，详见 docs/SECURITY-VALIDATION.md。

无人值守阶段采用上述默认技术选择。部署时仍需管理员提供：管理端固定 IP、管理密码、注册包密码、客户端机器、最终审阅后的白名单，以及 Windows 实机/VM 验收环境。安装向导收集实际参数，不在源码中写入通用密码或私钥。

## 6. 完成判据

本机交付：源码可编译、独立测试通过、生成实际 EXE 安装包、校验和和验证文档齐全。Windows 部署验收：另外按记录在 Windows 环境执行并观察实际结果；未执行项保持未验证。严格域名隔离及已删除驱动自动重建不纳入已实现能力，不以安装包生成代替功能验收。
